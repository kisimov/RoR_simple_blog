<div class="conteiner">
  <div class="row">
      <div class="col-md-5 col-sm-5">
      </div>
      <div class="col-md-2 col-sm-2 col-xs-12">
       

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= devise_error_messages! %>
          
          <h2 class="text-center">Sign up</h2>
          <br />

          <div class="field">
            <label class="sr-only" for="name">Email</label>
            <%= f.email_field :email, autofocus: true, class: 'form-control', placeholder: 'Email', id: "log_inp3" %>
          </div>
            <br />

          <div class="field">
            <label class="sr-only" for="name">Username</label>
            <%= f.text_field :username, class: 'form-control', placeholder: 'Username', id: "log_inp1"%>
          </div>
           <br />
           <br />

          <div class="field">
            <label class="sr-only" for="name">Password</label>
            <% if @minimum_password_length %>
            <em>(<%= @minimum_password_length %> characters minimum)</em>
            <% end %><br />
            <%= f.password_field :password, autocomplete: "off", class: 'form-control', placeholder: 'Password', id: "log_inp2"  %>
          </div>
            <br />

          <div class="field">
            <label class="sr-only" for="name">Password Confirmation</label>
            <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control', placeholder: 'Confirm Password', id: "log_inp2"   %>
          </div>
            <br />

          <div class="actions">
            <%= f.submit "Sign up", class: 'btn btn-primary btn-md btn-block', id: "btn_log" %>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
  </div>
</div>